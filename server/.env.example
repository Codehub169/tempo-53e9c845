# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# Relative path from server/src directory to the database file
DATABASE_URL=../data/apartments.db

# Client URL for CORS (during development if client is on a different port)
# In production, if backend serves client, this might be your main domain or not strictly needed for same-origin.
CLIENT_URL=http://localhost:9000

# Optional: Secret key for JWT or other security features if added later
# API_SECRET_KEY=your_super_secret_key
